{
  "setup-worktree-unix": [
    "pnpm install",
    "([ -f \"$ROOT_WORKTREE_PATH/.env\" ] && cp \"$ROOT_WORKTREE_PATH/.env\" .env) || true"
  ],
  "setup-worktree-windows": [
    "pnpm install",
    "if exist \"%ROOT_WORKTREE_PATH%\\.env\" copy \"%ROOT_WORKTREE_PATH%\\.env\" .env"
  ]
}
